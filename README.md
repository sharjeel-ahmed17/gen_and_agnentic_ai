# gen_and_agnentic_ai

gen_and_agnentic_ai

github repo create
use readme.md file
.gitignore (add *.pdf , *sqllite3 , database\ )
mit license

virtual envirment with python 3.12 version
uv venv --python 3.12 .venv
uv init
uv add <pakages_name>
uv add --dev <pakages_name>
uv pip freeze > requirements.txt
add command in powershell = $PYTHONENV = "."
.env.example
.env

runtime.txt
requirements.txt
requirement-dev.txt

Dockerfile
.dockerignore
docker-compose.yaml
